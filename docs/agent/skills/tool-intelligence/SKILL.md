# Tool Intelligence

- Dùng `tool.find_by_capability` để shortlist.
- Dùng `tool.get_guidance` để lấy risk/cost, prerequisites, follow-ups, anti-patterns.
- `TOOL_GRAPH.overlay.json` chứa curated hints, không thay live manifest.
- `TASK_TEMPLATES.md` chứa workflow mẫu để AI plan nhanh hơn.
