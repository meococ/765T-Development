# Sheet Documentation

- Sheet tốt = title block đúng, layout không rỗng, naming rõ, metadata đủ.
- AI nên ưu tiên structured sheet/schedule data trước khi đòi hình ảnh.
- Export/issue review phải kiểm tra title block, viewport count, schedule placement, sheet parameters.
